# Work Log 2026-08-13

실로봇 세션 준비. **Phase A(오프라인)를 완료**했고, Phase B(로봇에 신규 정책 배포 후
실측 기준 갱신)는 로봇이 켜지면 진행한다.

## shape10 실측 탐색

`~/Projects/shape10/outputs/`에 8/5 이후 학습 산출물 72개 run. 그중 **8/10~13 ONNX
export 완료분이 카탈로그 `planned` 21개와 매칭** (cohort 01 대부분 + dance +
CrowdCheer·ChefsKiss·CheckingTime 등). 카탈로그에 없던 3개(push_up, snu_cheer,
straykids)는 planned/llm:false 로 추가했다.

**핵심 리스크**: 로봇 state 이름은 shape10 이름에서 규칙 없이 변환된다. 매핑 테이블이
없고(변환 함수는 `run_motion_pipeline.py:278`의 gym task id 생성뿐, Mimic 이름과 또 다름),
기존 로봇 24개도 suffix 유지/제거 혼재. seed 충돌 위험: anse_kabanse **A459**(신규) vs
로봇 `MimicAnseKabanse`(**A456**), bow **A429**(신규, `MimicBowPolite`) vs 로봇
`MimicBow`(**A428**), body_stretch **A029**(신규, `MimicStretch`) vs 로봇
`MimicBodyStretch`(**A359**). → **실측 없이 승격·allowlist 확장 금지.** Phase B 1단계가
`tools/check_modes.sh` 인 이유다.

## Phase A 산출물

### A1. 대화 로깅 (persona 튜닝 근거)

- `session_log.py`: queue + 데몬 writer, JSONL `logs/<시각>-<rand>.jsonl`.
  `--no-transcripts` 면 발화 원문 대신 `<N자>`만 (행사장 프라이버시 모드)
- `server.py`: `--log-dir`/`--no-log`/`--no-transcripts`, `/event` POST(kind 화이트리스트,
  인증 게이트), `State.play()`가 `motion_request` 기록, `session_start`에 persona sha1 포함
- `static/index.html`: `turnId` — 입력 전사 완료에 증가+전송, 응답 전사 전송,
  `/motion`에 turn_id 동봉. 전송 실패는 조용히 무시
- **로봇 gateway는 건드리지 않았다** — 목적이 대화 기록이라 PC 로 충분
- `test_session_log.py` 6개: 왕복·seq, 프라이버시 모드 원문 미기록, 미인증 403,
  임의 kind 400, **발화와 동작이 같은 turn_id 로 묶임**(튜닝 루프의 핵심)

튜닝 루프: JSONL → turn_id 로 발화↔동작 묶어 오선택 확인 → 해당 desc 에
"언제 쓰지 않는지" 추가 → relay 재시작 → 재시도. PERSONA 는 전역 규칙 문제일 때만.

### A2. PERSONA 카테고리 개편

동작 3개 이름 하드코딩을 제거 — 동작이 늘 때마다 PERSONA 를 고쳐야 했고, 안 고치면
모델이 새 동작을 못 쓴다. 이제 "몸짓이 어울리는 발화 → 도구 목록에서 설명이 맞는 것
1회, 일반 대화 → 동작 없음(정상 선택)" 일반 규칙만. 개별 용도는 카탈로그 `desc` 가
담당(`build_tools()` 자동 주입). 거부 시 "실행된 척 말하지 않기" 규칙 추가.
기존 3개 체제 그대로 mock 검증 통과 — B 에서 enum 이 늘면 자동 확장.

### A3. `tools/check_modes.sh`

로봇 `list_modes` ↔ 카탈로그·allowlist 3분류 대조 (+`--sample` 오프라인 모드).
suffix 사례(`MimicCasualGreetingR001A428` → 유사: `MimicCasualGreeting`)로 검증 완료.
`Damping`/`ZeroPose` 는 의도적 미등록으로 별도 표기.

### A4. 카탈로그

- 신규 3개 추가 (planned, llm:false)
- 혼동 위험 가족군 desc 7건에 "언제 쓰지 않는지" 명시:
  인사 4건(캐주얼↔배웅↔정중↔궁정), 박수 2건(정중↔환호), 시간확인 1건
- `training_backlog` 에 shape10 export 확인 기록
- **status/allowlist 는 건드리지 않음** — Phase B 에서 실측 후

### A5. 문서

- `TEST_DIALOGUES.md` 신규 — 유도 발화(인사 가족군 구분 시험 포함), 네거티브, 경계,
  세션 후 로그 분석 원라이너
- `NEXT_SESSION_CHECKLIST.md` 전면 갱신 — B-0(정책 배포, 사람) → B-1(check_modes →
  이름 교정 → allowlist) → B-2(`--deploy`) → 정지 → 버튼 전수 → 음성 → 로그.
  "버튼은 /operator 전용, ready 전 잠김" 명시 (버튼 실종 오해의 답)

## 테스트

```text
python3 -m unittest test_gateway test_relay_backend test_server_tools test_ui_http \
                    test_ui_dispatch test_session_log
52 tests OK   (기존 46 + 신규 6)
```

## Phase B 실행 기록 (로봇 연결 후)

1. **자산 17개 로봇 배포** — shape10 outputs → 컨테이너 `assets/k1/mimic/`, md5 일치.
   config(`k1_config.yaml`)에 states/behaviors 17쌍 삽입 (백업 `.bak.motion17_20260813`).
   **state 이름은 배포자가 정하는 것이라 카탈로그와 동일하게 등록** — 이름 예측 불가 문제 해소.
2. `go` 재시작 후 `list_modes` **41개** 확인 (기존 24 + 신규 17, 이름 교정 대상 0).
3. 카탈로그 19개 planned→ready 승격, 대화형 3개 llm:true.
   allowlist: **api 33 / llm 18** (합의 경계). 테스트 53개 통과.
4. motion_llm 7파일 로봇 재배포, gateway 고정 토큰 재기동.
   호출 경로 검증: 신규 동작이 "SD API Arm이 켜져 있지 않습니다"까지 도달(= authority 직전
   전 구간 통과), Cartwheel 은 LLM 거부, 정지 경로 정상.
5. **RC 2페이지 20슬롯 전체 재구성** (백업 `.bak.rc_slots_20260813` → `.bak.rc_pages_20260813`,
   `go` 재시작 필요). 물리 확인: CH5 2단 스위치로 페이지 2개(input_code 4/5), CH11 다이얼로
   슬롯 200~209 — 3페이지째는 물리적으로 없다.
   배치 논리: 1페이지 = 관객 데모(검증 3 + 대화형 신규), 2페이지 = 스태프
   (**위험군 4 = RC 유일 경로** + LLM 비노출 퍼포먼스). RC에서 빠진 것들은 전부
   API 버튼(전부)·음성(대화형)으로 호출 가능하다.

### 현재 RC 지도 (2026-08-13 최종 — 이 표가 진실이다)

photopose 작업이 config 를 되돌려 재적용했고(`.bak.motion17_retry_*`, `.bak.rc_pages_retry_*`),
이후 사용자 요청으로 **2페이지를 신규 동작 전용으로** 다시 채웠다 (`.bak.page2new_20260813`).

| 슬롯 | 1페이지 · 관객 데모 | 2페이지(SA 홀드) · 신규 전용 |
|---|---|---|
| 200 | 체스트팝 v2 | 서울대 응원 |
| 201 | 캐주얼 인사 | 스키즈 |
| 202 | 배웅 인사 | 팔굽혀펴기 |
| 203 | 큰 박수 | 클릭 댄스 |
| 204 | 손 흔들기 | 재즈핸즈 |
| 205 | 배꼽 인사 | 플로스 |
| 206 | 이리 오세요 | 블라인딩 라이츠 |
| 207 | 진정하세요 | 시간 확인 |
| 208 | 손키스 | 팔짱 |
| 209 | 궁정식 인사 | 팔짱 턴 |

**신규 17개 전부 RC 에 올라갔다** (1페이지 7 + 2페이지 10).
**위험군 4개(옆돌기·섀도우복싱·복싱뉴턴·걷기)는 이제 어느 경로로도 호출 불가** —
api_allowlist 에 없고 RC 에서도 빠졌다. 다시 열려면 config 슬롯 복원이 필요하다.
photopose 는 로봇에서 완전히 빠져 있다 (sim2sim 에서 별도 테스트 예정).
카탈로그의 옛 `page`/`slot` 필드는 구 체계라 무시한다.

## 확정된 결정 (Phase B 에서 적용)

- LLM 제외 경계: 회전·긴 댄스·체조·미확인·이동 큰 것. 나머지 전부 개방 (~20개)
- 개방 시점: 처음부터 (오늘 세션이 곧 검증, E-stop 전제)
- 접촉 동작(SseSseSse 등)은 방법론 문서상 후순위 — B 에서 재확인
